# Local Development Setup

## Requirements

- Node.js 22+
- Docker Desktop
- PostgreSQL (Docker)
- Redis (Docker)

## Commands

Install dependencies

npm install

Run Docker

docker compose up -d

Run backend

npm run dev